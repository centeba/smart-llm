[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = ["python -m venv /opt/venv", ". /opt/venv/bin/activate", "pip install -e ."]

[start]
cmd = ". /opt/venv/bin/activate && uvicorn smart_llm.api.main:app --host 0.0.0.0 --port $PORT"
